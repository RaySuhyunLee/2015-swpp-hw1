<h1>User Accounts</h1>

<table>
	<tr>
		<th>Name</th>
		<th>Pass</th>
		<th>Count</th>
	</tr>

	<% @account_all.each do |account| %>
		<tr>
			<td><%= account.username %></td>
			<td><%= account.pass %></td>
			<td><%= account.count %></td>
		</tr>
	<% end %>
</table>
